{/* <div
  style={
    {
      // display: "flex",
      // flexDirection: "column",
      // justifyContent: "flex-end",
      // padding: "10px",
    }
  }
>
  <UnstyledButton
    onClick={() => {
      handleSignOut;
      navigate("/");
    }}
  >
    Sign Out
  </UnstyledButton>

  {currentUser ? <p>Welcome, {currentUser.email}</p> : <p>Welcome, Guest</p>}
</div>; */}
// temporary file for future implementation